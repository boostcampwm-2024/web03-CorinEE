export type CalculatePercentage = {
	percentage: number;
};

export type Trade = {
	message: string;
	statusCode: number;
};

export type CheckCoin = {
	message: string;
	statusCode: number;
	own: boolean;
};
