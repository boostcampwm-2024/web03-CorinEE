services:
  server:
    build:
      context: .
      dockerfile: dockerfile-server
    image: seunggwan/corinee-server
    restart: always
    env_file:
      - .env
    ports:
      - "3000:3000"
    networks:
      - app-network

  client:
    build:
      context: .
      dockerfile: dockerfile-client
    image: seunggwan/corinee-client
    ports:
      - "443:443"
    depends_on:
      - server
    volumes:
      - /etc/letsencrypt/live/corinee.site/fullchain.pem:/etc/nginx/ssl/certificate.crt
      - /etc/letsencrypt/live/corinee.site/privkey.pem:/etc/nginx/ssl/private.key
    networks:
      - app-network

networks:
  app-network:
    driver: bridge